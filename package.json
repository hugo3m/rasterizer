{
  "name": "nextjscpplate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon",
    "build": "rm -rf lib && cmake -B lib && cmake --build lib && next build",
    "start": "next start",
    "lint": "next lint",
    "test": "rm -rf lib && cmake -B lib && cmake --build lib && cd lib && ctest --output-on-failure && cd .."
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "14.2.13"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "eslint": "^8",
    "eslint-config-next": "14.2.13"
  }
}